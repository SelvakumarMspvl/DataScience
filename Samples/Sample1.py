from statistics import median

import  pandas as pd
import numpy as np

my_list = [1,2,3,4,5,6,7]
# length = len(my_list)
# list_df = pd.DataFrame(my_list)
# sum  = list_df.sum()
# avg = sum/ length
# avg = float(avg.iloc[0])
# print(avg)